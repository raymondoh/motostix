// __mocks__/next-auth/providers/credentials.js

// This module is a function that returns a provider configuration.
// We can just mock it with jest.fn() to prevent the syntax error.
module.exports = jest.fn();
